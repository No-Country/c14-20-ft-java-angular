export interface Product {
  idProduct: number;
  name: string;
  description: string;
  createdAt: string;
  updatedAt: string;
  price: number;
  stock: number;
  image: string;
  enable: boolean;
}
